/**
 * 
 */

$(document).ready(function() {
	// Trigg the apparition of menu bar
	/**$(window).bind('scroll',function () {
		if($(this).scrollTop() > 40) {
			$("#persistent-menu-wrapper").fadeIn(100);
		} else {
			$("#persistent-menu-wrapper").fadeOut(100);
		}
	});**/
});
